import 'package:flutter/material.dart';

class BillCircle extends StatelessWidget {
  static const String id = 'BillCircle';

  const BillCircle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Bill Circle'),
      ),
      body: Container(),
    );
  }
}
